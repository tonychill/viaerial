import { FC } from "react";

interface ListProps {
  type: string;
}
const List: FC<ListProps> = ({ type }) => {
  return (
    <div>
      <div>List</div>
    </div>
  );
};

export default List;

/*** Notes ***
 * Notes go here.
 */
